import { DecomposedTransform } from '../util/layout';

// ---- geometry helpers ------------------------------------------------------

export const IDENTITY_DECOMP: DecomposedTransform = { translateX: 0, translateY: 0, rotate: 0, skewX: 0, skewY: 0, scaleX: 1, scaleY: 1 };
